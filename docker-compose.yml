services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    ports:
      - "5431:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: hardware_crm
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
